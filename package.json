{
  "name": "fun-di",
  "version": "0.0.1",
  "description": "A DI library for factory functions with compile-time accumulative dependency type-checking.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:esm": "esbuild index.ts --bundle --format=esm --platform=node --target=node20 --outfile=dist/index.js --sourcemap",
    "build:types": "tsc -p tsconfig.build.json",
    "build": "npm run clean && npm run build:types && npm run build:esm",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run build",
    "test:watch": "vitest",
    "test": "vitest --run"
  },
  "keywords": [
    "dependency-injection",
    "di",
    "functional-di",
    "fun-di",
    "typescript",
    "typescript-di",
    "injection",
    "ioc",
    "lightweight",
    "framework-agnostic",
    "esm-only"
  ],
  "author": "André Vale <andreventuravale@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.25.9",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
